{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Refrain main window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
